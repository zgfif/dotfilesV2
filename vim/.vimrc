" highlight syntax
syntax on

set number
set relativenumber

" for command << and >> indetation 4 spaces
set shiftwidth=4

" replace tab with spaces
set expandtab

" one tab occupy 4 spaces
set tabstop=4

